source "https://rubygems.org"

setting = ->(name, default) { ENV[name].to_s.empty? ? default : ENV[name] }

gem "mongo", setting.("MONGO_DRIVER_VERSION", "2.26.0")
gem "railties", setting.("RAILS_VERSION", "8.1.3.1")
gem "json", "< 3"

gemspec path: "../"
